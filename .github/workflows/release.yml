name: Precompiled release
on:
  workflow_dispatch:
    inputs:
      create_draft:
        description: Create a GitHub draft release after all package checks pass
        type: boolean
        default: false
permissions:
  contents: read
concurrency:
  group: precompiled-release-${{ github.ref }}
  cancel-in-progress: false
jobs:
  build:
    permissions:
      contents: read
      id-token: write
      attestations: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: macos-14
            target: darwin-arm64
          - runner: macos-15-intel
            target: darwin-x64
          - runner: ubuntu-22.04
            target: linux-x64-gnu
          - runner: ubuntu-22.04-arm
            target: linux-arm64-gnu
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 45
    env:
      MDR_SIGNING_MODE: skip
      SHARP_IGNORE_GLOBAL_LIBVIPS: '1'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          submodules: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: .node-version
          cache: npm
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: '1.27.0'
          cache: false
      - name: Linux native build prerequisites
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y g++-12 gcc-12 make python3
          echo 'CC=gcc-12' >> "$GITHUB_ENV"
          echo 'CXX=g++-12' >> "$GITHUB_ENV"
      - run: npm ci
      - run: npm run test:all
      - run: npm run verify:cli
      - run: npm run release:check
      - name: Build pinned self-contained runtime package
        run: npm run release:bundle
      - name: Verify the extracted package without system development tools
        shell: bash
        run: |
          set -euo pipefail
          for archive in artifacts/*.tar.gz; do node scripts/release/verify.mjs "$archive"; done
      - name: Record build provenance (not OS publisher signing)
        uses: actions/attest-build-provenance@4d101475d8b20a2381f78447822ac1eab6504dd8 # v4.2.2
        with:
          subject-path: artifacts/*.tar.gz
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: bundle-${{ matrix.target }}
          path: |
            artifacts/*.tar.gz
            artifacts/*.sha256
            artifacts/*.verification.json
          if-no-files-found: error
          compression-level: 0
  windows:
    permissions:
      contents: read
      id-token: write
      attestations: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: windows-2025
            target: win32-x64
            arch: x64
          - runner: windows-11-arm
            target: win32-arm64
            arch: arm64
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 45
    env:
      MDR_SIGNING_MODE: skip
      SHARP_IGNORE_GLOBAL_LIBVIPS: '1'
      MDR_EXPECTED_WINDOWS_ARCH: ${{ matrix.arch }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          submodules: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: .node-version
          cache: npm
          architecture: ${{ matrix.arch }}
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: '1.27.0'
          cache: false
          architecture: ${{ matrix.arch }}
      - name: Require native Windows architecture
        shell: pwsh
        run: |
          if ([Runtime.InteropServices.RuntimeInformation]::OSArchitecture.ToString().ToLowerInvariant() -ne $env:MDR_EXPECTED_WINDOWS_ARCH) { throw 'Wrong Windows runner architecture.' }
          node -e "if(process.platform!=='win32'||process.arch!==process.env.MDR_EXPECTED_WINDOWS_ARCH)process.exit(1)"
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
      - run: npm ci --ignore-scripts --include=optional
      - run: npm run build:windows-host
      - run: go test -mod=readonly ./...
        working-directory: native/windows-host
      - run: npm run test:windows
      - run: npm run release:check
      - name: Build native Windows self-contained ZIP
        run: npm run release:bundle
      - name: Verify exact Windows ZIP without system development tools
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $archives = @(Get-ChildItem artifacts/*.zip)
          if ($archives.Count -ne 1) { throw 'Expected one native Windows archive.' }
          node scripts/release/verify.mjs $archives[0].FullName
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
      - name: Record build provenance (not OS publisher signing)
        uses: actions/attest-build-provenance@4d101475d8b20a2381f78447822ac1eab6504dd8 # v4.2.2
        with:
          subject-path: artifacts/*.zip
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: bundle-${{ matrix.target }}
          path: |
            artifacts/*.zip
            artifacts/*.sha256
            artifacts/*.verification.json
          if-no-files-found: error
          compression-level: 0
  assemble:
    needs: [build, windows]
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: .node-version
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: bundle-*
          merge-multiple: true
          path: artifacts
      - run: node scripts/release/aggregate.mjs
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-assets
          path: |
            artifacts/*.tar.gz
            artifacts/*.zip
            artifacts/SHA256SUMS
            artifacts/VERIFICATION.json
          if-no-files-found: error
          compression-level: 0
      - name: Create draft release only after all six native packages pass
        if: inputs.create_draft && github.ref == 'refs/heads/main'
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          version=$(node -p 'JSON.parse(require("fs").readFileSync("package.json")).version')
          gh release create "v$version" --target "$GITHUB_SHA" --title "v$version" --draft --notes-file "docs/releases/v$version.md"
          gh release upload "v$version" artifacts/*.tar.gz artifacts/*.zip artifacts/SHA256SUMS artifacts/VERIFICATION.json
