# Copy to ignored runtime.env. Do not commit real credentials.
# Values are parsed as dotenv data; no shell expansion or commands are evaluated.
CONTROL_PLANE_TUNNEL_ID=tunnel_00000000000000000000000000000000
CONTROL_PLANE_API_KEY=replace-with-your-own-runtime-key
# HTTPS_PROXY=http://127.0.0.1:8080
# NO_PROXY=localhost,127.0.0.1,::1
